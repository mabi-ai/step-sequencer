import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jeffersonh.stepsequencer',
  appName: 'Step Sequencer',
  webDir: 'dist'
};

export default config;
